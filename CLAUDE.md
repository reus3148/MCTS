# CLAUDE.md

이 파일은 Claude Code가 이 저장소에서 작업할 때 참고하는 지침서입니다.

## 프로젝트 개요

MCTS-ONC (Monte Carlo Tree Search for Oncology) — 경북대학교 의과대학 융합형 의사과학자 양성사업 학부과정 의과학 연구동아리 웹사이트.

연구 주제: 강화학습 기반 몬테카를로 트리 탐색을 활용한 유방암 치료 의사결정 모형 개발 및 표준 가이드라인 기반 정책과의 비교 연구.

## 왜 이 연구를 하는가 (작업 전 반드시 읽을 것)

**핵심 연구 질문** — 모든 설계·해석 판단의 기준이다.

> 현행 NCCN 유방암 치료 가이드라인은, 강화학습 에이전트가 자유 탐색으로 찾은 전략과
> 비교했을 때 최적인가?

**출발점은 체스다.** AlphaZero가 무한한 경우의 수를 전부 계산하지 않고 MCTS로 유망한
수만 탐색해 체스를 정복한 방식이, 유방암 치료 결정과 구조적으로 같은 **순차적 의사결정**
문제라는 관찰에서 시작했다. `지금 보드 → 최선의 수 → 결과` = `지금 환자 상태 → 최선의
치료 → 결과`.

**"만든다"가 아니라 "검증한다".** 처음 구상은 "AI로 최적 치료 전략을 새로 만들자"였으나
9개월·학부 리소스 제약을 고려해 방향을 틀었다. 2026-05-26 킥오프에서 치료 시점 정보
부재와 데이터 시기 mismatch에 대응해 frame을 다시 **"가이드라인 검증 PoC"** 로 좁혔다.

따라서 **이 프로젝트의 산출물은 "이기는 MCTS"가 아니라 "믿을 수 있는 비교"** 다.
이 구분이 실무에서 갖는 뜻:

- 두 정책 중 **한쪽에 근거 없는 이득을 주는 구조는 성능 문제가 아니라 최우선 결함**이다.
  비교가 기울면 MCTS가 이겨도 져도 연구 질문에 답할 수 없다.
- 특정 행동을 골라야만 발동하는 채널은 **기댓값이 중립(1.0)인지 산술로 검증**한다.
  중립이 아니면 그것은 선언되지 않은 임상 가정이므로, 재중심화하고 실제 이득은 별도
  파라미터로 분리해 민감도 분석 대상으로 만든다.
- **두 정책이 접근할 수 있는 채널이 같은지 확인한다**(v1.5). NCCN이 한 번도 쓰지 않는
  수준에만 이득이 붙어 있으면 그 이득은 MCTS 전용이다.
- **환경이 허용하는 수의 개수가 실제 기보와 맞는지 확인한다**(v1.6). 실제 5년 내
  레지멘 수 중앙값은 Stage I 2수, Stage III 5수, Stage IV 90백분위 9수다. 수를 적게
  두게 하면 순서를 최적화할 여지 자체가 줄어든다.
- **순차적 방법을 쓸 만큼 적응할 자리가 있는지 확인한다**(v1.7). 응답을 계획자에게서
  완전히 가려도 격차가 −0.0004(z = −0.29)만 움직였다 — 응답이 에피소드의 20.8%에서
  한 번만 나오기 때문이다. **적응할 자리가 없으면 MCTS는 정적 최적화기와 같다.**
- 지표가 좋아지면 먼저 "탐색이 시뮬레이터의 허점을 이용한 것 아닌가"를 의심한다.
  MCTS는 시뮬레이터의 목적함수를 직접 최적화하므로, 가정이 틀리면 잘 탐색할수록 더 틀린다.
- 결과를 좋게 포장하는 것보다 **한계를 정확히 적는 것이 이 연구의 기여**다.

**왜 유방암인가**: 공개 데이터가 가장 풍부하고(METABRIC/TCGA), 아형 분류가 명확해
State 정의가 쉬우며, 치료 옵션이 다양해 Action 설계가 풍부하다.

**기존 NCCN+AI 연구와의 차별점**: 현행 연구(진단 AI·LLM 추천·워크플로우 통합)는 모두
가이드라인을 **더 잘 따르도록** 돕는다. 이 연구는 가이드라인 **자체가 최적인지 검증**하고,
단일 시점 추천이 아니라 순차적 치료 전략 전체를 본다.

**장기 비전**: 알고리즘 엔진과 질환 환경을 분리한 모듈형 아키텍처로 위암·폐암 등 타
고형암으로 확장. `analysis/dynamic/schema.py`가 데이터셋 중립 스키마인 것과 K-CURE
어댑터를 따로 두는 설계가 모두 이 비전에서 나온다 — 환경에 METABRIC 전용 가정을 심지 말 것.

원문 근거: `유방암_RL_연구제안서.md`, `연구_로드맵.md`, `src/minutes/2026-05-26-kickoff.md`.

## 기술 스택

- **React 18** + **Vite 5** + **react-router-dom 7**
- 순수 CSS (CSS-in-JS / Tailwind 미사용)
- 배포: **Vercel** (GitHub main 브랜치 push 시 자동 배포)

## ⚠️ 호환성 요구사항 (필수)

**모바일과 PC 양쪽 모두에서 완벽하게 작동해야 합니다.**

- 모든 페이지·컴포넌트는 `≤ 640px` (모바일), `≤ 768px` (태블릿/햄버거 메뉴 전환점), `≥ 769px` (데스크탑) 세 구간에서 깨짐 없이 표시되어야 함
- 새 컴포넌트를 만들 때 반드시 모바일 폭(360~414px) 기준에서 가독성·터치 영역·줄바꿈을 검증할 것
- 텍스트 가로 오버플로우, 가로 스크롤, 글자 잘림은 절대 허용 금지
- 그리드/플렉스 레이아웃은 `grid-template-columns: repeat(auto-fit, minmax(...))` 또는 미디어 쿼리로 모바일에서 단일 열로 전환
- 폰트 크기는 `clamp()` 또는 미디어 쿼리로 모바일에서 축소
- Navbar는 768px 이하에서 햄버거 메뉴로 전환

## 폴더 구조

```
src/
├── components/        # 재사용 컴포넌트 (Navbar, Hero, Research, Roadmap, Team, Footer)
├── pages/             # 라우터 페이지 (Home, BreastCancerPage, MCTSPage, ResearchPage, RoadmapPage, TeamPage)
├── App.jsx            # BrowserRouter + Routes
├── main.jsx           # 엔트리포인트
└── index.css          # 전역 변수·section 클래스
```

각 컴포넌트는 동일 이름의 `.css` 파일을 함께 둠 (예: `Hero.jsx` + `Hero.css`).

## 연구 코드 (Python) — 웹사이트와 별개

이 저장소는 **역할이 둘**이다. 위 `src/`(React 블로그, `mcts.blundermate.app`으로 배포)와,
아래 `analysis/`의 **MCTS 연구 파이프라인**(로컬에서 `py`로 실행, 배포되지 않음).

```
analysis/
├── 01~05_*.py       # METABRIC 전처리·시각화·NCCN 정책 일치율
├── 06~07_*.py       # MCTS 정적 PoC v1 (Cox 보상모형 + UCT 탐색)
├── 08~09_*.py       # 동적 확률환경 v0.2 (chance-aware UCT)
├── 10~11_*.py       # 다중 시드 강건성·합성 가정 민감도 v0.3
├── 12_*.py          # 탐색 예산 스케일링 진단 v0.4
├── 37~38_*.py       # GENIE BPC 자료 적격성 평가 v1.6
├── 39~40_*.py       # 폐루프 적응 분리 v1.7
├── mcts/            # 치료환경·보상모형·UCT 탐색 모듈
├── genie/           # GENIE BPC 어댑터 (loader.py·sequences.py)
├── causal/          # 프로펜서티·트리밍·IPCW·AIPW
└── dynamic/         # 공통 스키마·확률 전이·stochastic MCTS
                     #   cohort.py: 10~12 공유 코호트·보상모형·매니페스트
                     #   experiment_utils.py: 테스트 가능한 통계 헬퍼
                     #   blinding.py: 응답 채널을 계획자에게서 가림 (v1.7)
tests/               # 단위 테스트 (py -m unittest discover -s tests -v)
reports/             # 재현 가능한 기술 리포트 (metrics·manifest·표·figure)
```

- **현재 상태·실행법·다음 단계·해석 경계**는 `README.md`, `PROJECT_TIMELINE.md`,
  `reports/*/README.md`, `src/minutes/`(날짜별 회의록)에 정리되어 있음 — 연구 작업 전 반드시 참고.
- **데이터**(`data/`)는 대용량이라 git 제외. 이 PC엔 `data/processed/*.csv`가 있어 바로 실행됨.
  새 환경에서는 `analysis/01~02`로 재생성 필요.
- **외부 자료**(`data/external/`, git 제외):
  - `MCTS_data/` — **GENIE BPC Breast Cancer v1.0-public**. 실제 환자 자료이고
    **재배포 금지**다. 저장소·사이트에 원자료를 올리지 않는다. 접근은 Synapse에서
    이용약관 동의 후. 리포트에는 파일별 SHA-256만 남긴다.
  - `kcure_datafreebox/` — K-CURE 유방암 표준 테이블의 **가짜 데이터**(7개 병원).
    스키마는 진짜인데 **값은 난수**다(ECOG가 0~4 균등, BMI 중앙값 99.11=결측코드).
    **분석에 쓰지 말고 어댑터 배관 검증에만 쓴다.**
  - CSV 인코딩이 파일마다 `utf-8` / `utf-8-sig` / `cp949`로 섞여 있다.
    `analysis/genie/loader.py`의 `read_csv`가 순서대로 시도한다 — 새 어댑터도 그걸 쓴다.
- 연구 코드 초안은 주로 Codex/Claude로 작성했고, 임상 규칙·인과추론·결과 해석은 사람 검토 대상(회의록에 명시).
- **실험 규약**: 번호 붙은 `analysis/1x_*.py`는 모듈로 import할 수 없으므로, 재사용·테스트
  대상 로직은 `analysis/dynamic/`에 두고 스크립트는 얇게 유지한다. 새 실험은
  `reports/<label>/`에 `metrics.json`·`run_manifest.json`·`tables/`·`README.md`를 함께 낸다.
- **탐색 예산**: v0.4 진단 이후 기본값은 **1024 이상**(256은 행동 순서를 분해하지 못함).
## 연구 작업의 완료 기준 (Definition of Done — 반드시 지킬 것)

이 프로젝트의 기록은 **그대로 논문이 되어야 한다.** 따라서 실험을 돌린 것만으로는
작업이 끝나지 않는다. 새 실험·분석을 하나 마칠 때마다 **아래 다섯 가지를 모두** 남긴다.
하나라도 빠지면 그 작업은 미완성이다.

| # | 산출물 | 위치 | 목적 |
|---|---|---|---|
| 1 | **일지** — 결정·근거·기각한 대안 | `src/minutes/<날짜>-<슬러그>.md` | 왜 그렇게 했는지 복원 |
| 2 | **기술 리포트** — 설계·수치·한계 | `reports/<label>/README.md` + `metrics.json`·`run_manifest.json`·`tables/` | 재현·검증 |
| 3 | **그림 최소 1장** | `reports/<label>/figures/` → `public/figures/figNN_*.png` 복사 | 발표에서 말로 못 하는 것 |
| 4 | **이야기 갱신** | `docs/research-story.md` | 8편을 읽지 않아도 줄기가 보이게 |
| 5 | **숫자 화해** — 헤드라인 수치가 바뀌면 | `docs/results-reconciliation.md` | "그래서 숫자가 뭔데"에 답 |

추가 규칙:

- **그림 없이 리포트를 끝내지 않는다.** figure 번호는 이어서 매기고(`figNN_`),
  `analysis/1x_*.py`가 표를 내면 대응하는 시각화를 같은 커밋에 넣는다. 스타일은
  `analysis/09`·`analysis/14`의 rcParams를 그대로 쓴다(한글 폰트·색 팔레트 통일).
- **회의록에 그림을 삽입한다**: `![설명](/figures/figNN_name.png)`. 사이트가 그대로 렌더한다.
- **헤드라인 수치가 바뀌면 이유를 함께 적는다.** 예산·표본·시드·환경 중 무엇이 바뀌어서
  달라졌는지 명시하지 않으면 "숫자가 오락가락한다"로 읽힌다.
- **Q&A에서 맞을 숫자를 미리 꺼내 둔다.** 예: Cox 보상모형 held-out C-index = 0.677은
  심사자가 반드시 묻는 값이므로 한계와 함께 표에 노출한다.
- **기각한 가설도 기록한다.** 무엇을 의심했다가 실측으로 아니라고 확인했는지가
  같은 의심의 재실행을 막는다.
- **전부 사이트에 공개해도 되는 톤으로 쓴다.** 단, `팀_내부_노트.md`의 비공개 항목
  (사적 관계·팀 역량 자평·예산 정정·자문 경위)은 어떤 산출물에도 넣지 않는다.

### 논문 수준이 요구하는 것 (기술 리포트에 반드시 포함)

- **추정 대상(estimand)을 한 문장으로** — 무엇을, 어떤 모집단에서, 어떤 대비로 재는가.
  분석 중 모집단이 바뀌면(예: propensity 겹침 구간 트리밍) 그 사실을 명시한다.
- **식별 가정과 위반 시 결과** — 교환가능성·positivity·일관성·무정보 검열 중 무엇을
  가정했는지, 깨지면 어떻게 되는지. 진단(SMD·겹침·유효표본)을 수치로 남긴다.
- **부정 결과는 검정력과 함께** — "차이 없음"과 "검출할 수 없었음"은 다르다. 분해 가능한
  최소 효과크기를 계산해 붙인다(`reports/environment-fix-v0.5`가 선례).
  `experiment_utils.minimum_detectable_difference(se)`가 (1.96+0.84)·SE를 낸다.
  **부호만 맞은 결과를 "통과"로 적지 않는다** — v1.3·v1.7이 "형식상 통과, 실질은 0"으로
  적은 것이 선례다.
- **민감도 분석** — 미측정 교란(E-value), 설계 선택(트리밍·절단·예산), 가정 흔들기.
- **재현 절차** — 입력 SHA-256·시드·커밋을 매니페스트에 남기고 실행 명령을 적는다.
- **참고문헌** — 방법을 빌려 왔으면 원 논문을 단다.
- **한계를 심각도 순으로**, 문서 끝이 아니라 해당 결과 가까이에.
- **사전 결정과 사후 결정을 구분** — 데이터를 보고 정한 기준은 그렇게 정했다고 쓰고
  대안도 함께 보고한다.
- **그림의 수치는 committed table·metrics.json에서 읽는다.** 손으로 옮겨 적지 않는다.

## 실험 설정 규약

- **환경 설정**: 새 실험은 `configs/dynamic_v0_5.json`을 쓴다. `dynamic_poc_v0_2.json`은
  v0.2~v0.4 리포트의 매니페스트 해시를 보존하기 위해 그대로 둔다(그 결과들은 매니페스트의
  `git_commit_before_run`으로 재현). 두 파일의 차이는 응답 채널 중립화 스위치와 명시적
  timing·할인율 선언이다 — `reports/environment-fix-v0.5` 참고.
- **민감도 최소 설계**: **시드 12개 · 예산 1024 · 환자 40명**(v1.0~v1.2). 3시드도,
  8명도, **20명 한 코호트도** 순위를 인용할 수 없다 — 겹치지 않는 두 코호트 간 순위
  Spearman이 +0.03이었다. 순위는 **순서가 아니라 그룹**으로 말한다.
- **코호트 표집**: `balanced_subtype_sample(..., offset=N)`으로 겹치지 않는 코호트를
  뽑는다. 중첩 코호트의 일치는 재현이 아니라 재계수다.
- **예측은 실행 전에 코드에 적고** `metrics.json`의 `prespecified_prediction`으로 낸다.
  나중에 적었다면 그냥 넘어갔을 문제를 잡는다(v1.2가 선례 — 예측이 빗나갔고, 그 덕에
  아형 표집 문제를 찾았다).

### 효용 격차 인용 규칙 (2026-09-05 결정, 강제)

헤드라인은 **아형 균등 표본 값**을 쓴다. 근거는 우리 추정 대상이 모집단 평균이 아니라
**결정 상황별 비교**이고, 표준화의 기준이 될 METABRIC 분포는 이미 외적 타당성이 없다고
선언한 분포이며, 유병률 가중이면 드문 아형에 대해 아무 말도 못 하기 때문이다.
전문·되돌릴 조건: `src/minutes/2026-09-05-headline-sampling-decision.md`.

**요약·발표·초록에서 인용할 때는 언제나 셋을 함께 적는다.**

> **+0.0199** (예산 1024 · 환자 40명 · 시드 12 · **아형 균등 표본 · 치료 중립 보상모형**)
> — 실제 아형 구성으로 표준화까지 하면 **+0.0136**
> — 아형에 따라 **3.7배** 갈린다 (HR+/HER2− +0.0136 ~ HR−/HER2+ +0.0503)

꼬리표 없이 +0.0199만 쓰는 것은 **금지**한다 — 독자가 모집단 평균으로 읽는다.
마찬가지로 **"모든 환자에게 낫다"고 쓰지 않는다**(40명 중 1명이 음수).

**보상모형은 예후만 담당한다**(v1.4). Cox 모형이 관찰자료에서 학습한 치료 계수는
미선언 채널이었고 NCCN을 깎고 있었다(격차의 40%, z = −5.37). 새 실험은
`neutralise_treatment_terms()`를 쓰고, 치료 효과는 `configs/dynamic_v0_5.json`의
선언된 파라미터가 담당한다. v0.2~v1.2의 수치는 옛 환경의 기록이다.

## 디자인 시스템

CSS 변수는 `src/index.css`의 `:root`에 정의:
- `--bg-dark`, `--bg-card`, `--bg-card2` — 배경 단계
- `--accent` (#00c9b1 청록), `--accent2` (#3b82f6 파랑) — 포인트
- `--text-primary`, `--text-secondary` — 텍스트
- `--border` — 카드/구분선

공통 섹션 헤더는 `.section-label` (대문자 라벨) + `.section-title` + `.section-desc` 3종 구조.

## 팀 정보 (사이트와 일치 필수)

- **전병우** (대표, 의학과 2020110089) — MDP 설계, 외부 컨택, 논문 의학 파트
- **강홍준** (팀원, 의학과 2021113048) — 코딩, 모델 학습, 실험 설계
- **서재환** (팀원, 의학과 2021110282) — 데이터 전처리, 가상 환자 환경 구축, 결과 시각화

## 비공개 정보 (웹사이트 노출 금지)

`팀_내부_노트.md`는 대외비 문서로 git에 커밋되어 있긴 하지만 다음 정보는 사이트에 반영하지 않음:
- 자문 시도 실패 사실
- 팀 코딩 수준 자평
- 예산 정정 내역
- 특정 임상의/병원과의 사적 관계

## 배포 워크플로우

```bash
git add .
git commit -m "..."
git push   # → Vercel 자동 배포
```

GitHub Pages는 사용하지 않음. `vercel.json`이 SPA 라우팅을 처리.
